<UserControl x:Class="SensorPanelToo.Controls.ThemeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="主题列表" Foreground="#333" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
            <Grid Grid.Row="1" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="ThemeCountLabel" Foreground="#888" FontSize="11" VerticalAlignment="Center"/>
                <Button x:Name="RefreshBtn" Grid.Column="1" Content="刷新" Width="44" Height="20" FontSize="10"
                        Click="RefreshBtn_Click"/>
            </Grid>

            <ListBox x:Name="ThemeListBox" Grid.Row="2" Background="White" BorderBrush="#CCC" BorderThickness="1"
                     SelectionChanged="ThemeListBox_SelectionChanged"
                     MouseDoubleClick="ThemeListBox_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="FontSize" Value="12"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>

        <Border Grid.Column="1" Margin="10,0,0,0" Background="White" BorderBrush="#DDD" BorderThickness="1">
            <Viewbox Stretch="Uniform">
                <Canvas x:Name="PreviewCanvas" Width="640" Height="360" Background="#0A0A0F" ClipToBounds="True">
                    <Image x:Name="BgImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Panel.ZIndex="-100" IsHitTestVisible="False"/>
                </Canvas>
            </Viewbox>
        </Border>
    </Grid>
</UserControl>
